#!/bin/bash

#@ job_name     = mpi_convex_hull
#@ initialdir   = /home/zanellas
#@ input        = /dev/null
#@ output       = $(job_name).out
#@ error        = $(job_name).err
#@ class        = short
#@ job_type     = parallel
#@ blocking     = unlimited
#@ total_tasks  = 4
#@ queue

bin/mpi_convex_hull
